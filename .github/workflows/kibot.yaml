name: KiBot

on:
  push:
    branches:
    - main
    paths:
    - 'kicad/**'
    - '.github/workflows/kibot.yaml'
  pull_request:
    paths:
    - 'kicad/**'
    - '.github/workflows/kibot.yaml'

jobs:
  kibot:
    runs-on: ubuntu-22.04
    container: ghcr.io/inti-cmnb/kicad7_auto:latest@sha256:35115ab476f5298de08e0f2dae3809f76b20f365c1e7959f23939f219ad01276
    steps:
    - uses: actions/checkout@v4
    - name: Cache 3D models data
      id: models-cache
      uses: set-soft/cache@main
      with:
        path: ~/cache_3d
        key: cache_3d
    - name: KiBot
      uses: INTI-CMNB/KiBot@v2_k8
      with:
        config: kicad/config.kibot.yaml
        schema: kicad/sneksafe.kicad_sch
        board: kicad/sneksafe.kicad_pcb
        cache3D: YES
        dir: Generated
    - name: Upload results
      uses: actions/upload-artifact@v4
      if: ${{ always() }}
      with:
        name: KiBot
        path: Generated
